<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.feduwacomm.mapper.UserMapper">
    
    <!-- 结果映射 -->
    <resultMap id="UserResultMap" type="com.feduwacomm.entity.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>
    
    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        id, username, email, phone, status, create_time, update_time
    </sql>
    
    <!-- 查询所有用户 -->
    <select id="findAll" resultMap="UserResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM users 
        WHERE status = 1
        ORDER BY create_time DESC
    </select>
    
    <!-- 根据ID查询用户 -->
    <select id="findById" parameterType="java.lang.Long" resultMap="UserResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM users 
        WHERE id = #{id}
    </select>
    
    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" parameterType="java.lang.String" resultMap="UserResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM users 
        WHERE username = #{username}
    </select>
    
    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.feduwacomm.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (
            username, 
            email, 
            phone, 
            status, 
            create_time, 
            update_time
        ) VALUES (
            #{username}, 
            #{email}, 
            #{phone}, 
            #{status}, 
            #{createTime}, 
            #{updateTime}
        )
    </insert>
    
    <!-- 更新用户 -->
    <update id="update" parameterType="com.feduwacomm.entity.User">
        UPDATE users 
        SET 
            username = #{username},
            email = #{email},
            phone = #{phone},
            status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>
    
    <!-- 删除用户（软删除） -->
    <update id="deleteById" parameterType="java.lang.Long">
        UPDATE users 
        SET status = 0, update_time = NOW() 
        WHERE id = #{id}
    </update>
    
    <!-- 统计用户总数 -->
    <select id="count" resultType="java.lang.Integer">
        SELECT COUNT(*) 
        FROM users 
        WHERE status = 1
    </select>
    
    <!-- 根据条件查询用户（可选，用于扩展） -->
    <select id="findByCondition" parameterType="map" resultMap="UserResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM users 
        WHERE status = 1
        <if test="username != null and username != ''">
            AND username LIKE CONCAT('%', #{username}, '%')
        </if>
        <if test="email != null and email != ''">
            AND email LIKE CONCAT('%', #{email}, '%')
        </if>
        <if test="phone != null and phone != ''">
            AND phone LIKE CONCAT('%', #{phone}, '%')
        </if>
        ORDER BY create_time DESC
    </select>
    
</mapper> 
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.feduwacomm.mapper.LogExportTaskMapper">

    <!-- 结果映射 -->
    <resultMap id="LogExportTaskResultMap" type="com.feduwacomm.entity.LogExportTask">
        <id property="id" column="id"/>
        <result property="exportId" column="export_id"/>
        <result property="status" column="status"/>
        <result property="format" column="format"/>
        <result property="filterConditions" column="filter_conditions"/>
        <result property="progress" column="progress"/>
        <result property="totalRecords" column="total_records"/>
        <result property="processedRecords" column="processed_records"/>
        <result property="fileSize" column="file_size"/>
        <result property="filePath" column="file_path"/>
        <result property="downloadUrl" column="download_url"/>
        <result property="estimatedTime" column="estimated_time"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="startedAt" column="started_at"/>
        <result property="completedAt" column="completed_at"/>
        <result property="errorMessage" column="error_message"/>
        <result property="createdBy" column="created_by"/>
        <result property="includeDetails" column="include_details"/>
    </resultMap>

    <!-- 插入日志导出任务 -->
    <insert id="insert" parameterType="com.feduwacomm.entity.LogExportTask">
        INSERT INTO log_export_tasks (
            id, export_id, status, format, filter_conditions, progress,
            total_records, processed_records, file_size, file_path, download_url,
            estimated_time, expires_at, created_at, started_at, completed_at,
            error_message, created_by, include_details
        ) VALUES (
            #{id}, #{exportId}, #{status}, #{format}, #{filterConditions}, #{progress},
            #{totalRecords}, #{processedRecords}, #{fileSize}, #{filePath}, #{downloadUrl},
            #{estimatedTime}, #{expiresAt}, #{createdAt}, #{startedAt}, #{completedAt},
            #{errorMessage}, #{createdBy}, #{includeDetails}
        )
    </insert>

    <!-- 根据ID查询导出任务 -->
    <select id="selectById" resultMap="LogExportTaskResultMap">
        SELECT * FROM log_export_tasks WHERE export_id = #{exportId}
    </select>

    <!-- 更新任务状态 -->
    <update id="updateStatus">
        UPDATE log_export_tasks
        SET status = #{status},
            progress = #{progress},
            processed_records = #{processedRecords},
            file_size = #{fileSize},
            completed_at = #{completedAt},
            error_message = #{errorMessage}
        WHERE export_id = #{exportId}
    </update>

    <!-- 更新任务进度 -->
    <update id="updateProgress">
        UPDATE log_export_tasks
        SET progress = #{progress},
            processed_records = #{processedRecords}
        WHERE export_id = #{exportId}
    </update>

    <!-- 根据状态查询导出任务 -->
    <select id="selectByStatus" resultMap="LogExportTaskResultMap">
        SELECT * FROM log_export_tasks
        <where>
            <if test="status != null">
                status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>

    <!-- 根据状态统计数量 -->
    <select id="countByStatus" resultType="long">
        SELECT COUNT(*) FROM log_export_tasks
        <where>
            <if test="status != null">
                status = #{status}
            </if>
        </where>
    </select>

    <!-- 根据创建者查询导出任务 -->
    <select id="selectByCreatedBy" resultMap="LogExportTaskResultMap">
        SELECT * FROM log_export_tasks
        WHERE created_by = #{createdBy}
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>

    <!-- 根据创建者统计数量 -->
    <select id="countByCreatedBy" resultType="long">
        SELECT COUNT(*) FROM log_export_tasks
        WHERE created_by = #{createdBy}
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <!-- 删除过期任务 -->
    <delete id="deleteExpiredTasks">
        DELETE FROM log_export_tasks
        WHERE expires_at &lt; #{expirationTime}
    </delete>

    <!-- 查询过期任务 -->
    <select id="selectExpiredTasks" resultMap="LogExportTaskResultMap">
        SELECT * FROM log_export_tasks
        WHERE expires_at &lt; #{expirationTime}
        ORDER BY expires_at ASC
    </select>

</mapper>
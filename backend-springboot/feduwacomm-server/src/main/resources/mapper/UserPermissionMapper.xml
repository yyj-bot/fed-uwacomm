<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.feduwacomm.mapper.UserPermissionMapper">

    <!-- 结果映射 -->
    <resultMap id="UserPermissionResultMap" type="com.feduwacomm.entity.UserPermission">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="resource_type" property="resourceType"/>
        <result column="resource_id" property="resourceId"/>
        <result column="permission" property="permission"/>
        <result column="granted_at" property="grantedAt"/>
        <result column="granted_by" property="grantedBy"/>
        <result column="expires_at" property="expiresAt"/>
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        id, user_id, resource_type, resource_id, permission,
        granted_at, granted_by, expires_at
    </sql>

    <!-- 根据ID查询权限 -->
    <select id="selectById" resultMap="UserPermissionResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_permissions
        WHERE id = #{id}
    </select>

    <!-- 根据用户ID查询所有权限 -->
    <select id="selectByUserId" resultMap="UserPermissionResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_permissions
        WHERE user_id = #{userId}
        AND (expires_at IS NULL OR expires_at > NOW())
        ORDER BY granted_at DESC
    </select>

    <!-- 根据用户ID和资源类型查询权限 -->
    <select id="selectByUserIdAndResourceType" resultMap="UserPermissionResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_permissions
        WHERE user_id = #{userId}
        AND resource_type = #{resourceType}
        AND (expires_at IS NULL OR expires_at > NOW())
        ORDER BY granted_at DESC
    </select>

    <!-- 根据用户ID、资源类型和资源ID查询权限 -->
    <select id="selectByUserIdAndResource" resultMap="UserPermissionResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_permissions
        WHERE user_id = #{userId}
        AND resource_type = #{resourceType}
        AND (resource_id = #{resourceId} OR resource_id IS NULL)
        AND (expires_at IS NULL OR expires_at > NOW())
        ORDER BY resource_id DESC, granted_at DESC
        LIMIT 1
    </select>

    <!-- 查询所有权限 -->
    <select id="selectAll" resultMap="UserPermissionResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_permissions
        ORDER BY granted_at DESC
    </select>

    <!-- 插入权限 -->
    <insert id="insert" parameterType="com.feduwacomm.entity.UserPermission">
        INSERT INTO user_permissions (
            id, user_id, resource_type, resource_id, permission,
            granted_at, granted_by, expires_at
        ) VALUES (
            #{id}, #{userId}, #{resourceType}, #{resourceId}, #{permission},
            #{grantedAt}, #{grantedBy}, #{expiresAt}
        )
    </insert>

    <!-- 更新权限 -->
    <update id="update" parameterType="com.feduwacomm.entity.UserPermission">
        UPDATE user_permissions
        SET resource_type = #{resourceType},
            resource_id = #{resourceId},
            permission = #{permission},
            expires_at = #{expiresAt}
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除权限 -->
    <delete id="deleteById">
        DELETE FROM user_permissions WHERE id = #{id}
    </delete>

    <!-- 根据用户ID删除所有权限 -->
    <delete id="deleteByUserId">
        DELETE FROM user_permissions WHERE user_id = #{userId}
    </delete>

    <!-- 根据用户ID和资源类型删除权限 -->
    <delete id="deleteByUserIdAndResourceType">
        DELETE FROM user_permissions 
        WHERE user_id = #{userId} AND resource_type = #{resourceType}
    </delete>

    <!-- 检查用户是否有指定权限 -->
    <select id="checkPermission" resultType="int">
        SELECT COUNT(*)
        FROM user_permissions
        WHERE user_id = #{userId}
        AND resource_type = #{resourceType}
        AND (resource_id = #{resourceId} OR resource_id IS NULL)
        AND permission = #{permission}
        AND (expires_at IS NULL OR expires_at > NOW())
    </select>

    <!-- 统计用户权限数量 -->
    <select id="countByUserId" resultType="int">
        SELECT COUNT(*)
        FROM user_permissions
        WHERE user_id = #{userId}
        AND (expires_at IS NULL OR expires_at > NOW())
    </select>

</mapper> 
# 水声联邦学习系统 HTTP 接口导览

## 1. 概述

本文档定义了水声联邦学习系统的HTTP REST API接口，包括联邦学习任务管理、训练数据管理、模型版本管理等功能。

### 1.1 基础信息
- **基础URL**: `http://localhost:8080/api`
- **API版本**: v1.0
- **认证方式**: JWT Token
- **数据格式**: JSON

### 1.2 响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

## 2. 数据库表结构设计

详细的数据库表结构设计请参考：[数据库表结构文档](../../database/database_schema.md)

本文档包含以下数据库表：
- **用户表 (users)**: 存储用户基本信息和身份认证
- **用户权限表 (user_permissions)**: 存储用户细粒度权限配置
- **联邦学习任务表 (federated_tasks)**: 存储任务配置和状态信息
- **训练数据集元信息表 (training_dataset)**: 存储训练数据集的元信息（如名称、描述、类型、状态等）
- **训练数据明细表 (training_dataset_row)**: 存储每个数据集的明细数据，每行为一条JSON格式记录，实现宽表+JSON灵活存储
- **模型版本表 (model_versions)**: 存储模型版本和性能指标
- **SpringBoot系统日志表 (system_logs)**: 存储SpringBoot应用运行日志
- **虚拟机运行日志表 (vm_runtime_logs)**: 存储虚拟机运行日志

## 3. API接口分类

### 3.1 用户管理接口

用户管理相关的API接口已独立拆分到：[用户管理API参考文档](./user-api-reference.md)

该文档包含以下接口：
- 用户注册接口
- 用户登录接口
- 用户信息管理接口
- 用户权限管理接口
- 管理员用户管理接口
- 密码管理接口

### 3.2 虚拟机管理接口

虚拟机管理相关的API接口已独立拆分到：[虚拟机API参考文档](./vm-api-reference.md)

该文档包含以下接口：
- 虚拟机注册接口
- 虚拟机列表查询接口
- 虚拟机详情查询接口
- 虚拟机更新接口
- 虚拟机删除接口
- 虚拟机控制接口（启动、停止、重启）
- 虚拟机状态查询接口

### 3.3 联邦学习任务管理接口

联邦学习任务管理相关的API接口已独立拆分到：[联邦学习任务管理API参考文档](./federated-task-api-reference.md)

该文档包含以下接口：
- 任务创建接口
- 任务配置接口
- 任务状态查询接口
- 任务控制接口（开始、暂停、停止、恢复、取消）
- 任务结果查询接口
- 任务日志查询接口
- 任务删除接口

### 3.4 训练数据管理接口

训练数据管理相关的API接口已独立拆分到：[训练数据管理API参考文档](./training-data-api-reference.md)

该文档包含以下接口：
- 文件上传接口
- 文本信息上传接口
- 数据列表查询接口
- 数据详情查询接口
- 数据下载接口
- 数据预处理接口
- 数据验证接口
- 数据更新接口
- 数据删除接口
- 批量数据操作接口
- 数据统计接口
- 数据导出接口

### 3.5 模型版本管理接口

模型版本管理相关的API接口已独立拆分到：[模型版本管理API参考文档](./model-version-api-reference.md)

> 提示：自当前版本起，模型版本仅以`parameters`(JSON)记录所有模型相关信息，不再包含`modelPath`、`modelSize`、`modelType`等字段；如需二进制传输，请使用专门的文件通道。

该文档包含以下接口：
- 模型上传接口（单文件/批量）
- 模型版本查询接口（列表/详情/任务相关）
- 模型性能评估接口（单模型/批量评估）
- 模型部署接口（部署/状态查询/列表）
- 模型回滚接口（回滚/历史查询）
- 模型下载接口（单文件/批量）
- 模型删除接口（单版本/批量）
- 模型统计接口（全局统计/任务统计）

### 3.6 系统日志接口

系统日志管理相关的API接口已独立拆分到：[系统日志管理API参考文档](./system-log-api-reference.md)

该文档包含以下接口：
- 日志查询接口（列表/详情/实时/统计）
- 日志导出接口（导出/状态查询/文件下载/历史查询）
- 日志清理接口（清理/状态查询/历史查询）
- 系统监控接口（系统状态/日志监控/性能监控/告警配置）
- 日志配置接口（配置查询/配置更新）

## 4. 错误码定义

### 4.1 通用错误码
| 错误码 | HTTP状态码 | 说明 |
|--------|------------|------|
| 200 | 200 | 成功 |
| 400 | 400 | 请求参数错误 |
| 401 | 401 | 未授权，需要登录 |
| 403 | 403 | 禁止访问，权限不足 |
| 404 | 404 | 资源不存在 |
| 409 | 409 | 资源冲突，已存在 |
| 422 | 422 | 请求参数验证失败 |
| 500 | 500 | 服务器内部错误 |
| 502 | 502 | 网关错误 |
| 503 | 503 | 服务不可用 |

### 4.2 业务错误码
| 错误码 | 说明 |
|--------|------|
| TASK_NOT_FOUND | 任务不存在 |
| TASK_ALREADY_EXISTS | 任务已存在 |
| TASK_INVALID_STATUS | 任务状态错误 |
| DATA_NOT_FOUND | 数据不存在 |
| DATA_UPLOAD_FAILED | 数据上传失败 |
| MODEL_NOT_FOUND | 模型不存在 |
| MODEL_VERSION_ERROR | 模型版本错误 |
| LOG_QUERY_FAILED | 日志查询失败 |

## 5. 安全规范

### 5.1 认证授权
- 所有API接口都需要JWT Token认证
- Token过期时间为24小时
- 支持Token刷新机制

### 5.2 数据验证
- 所有输入参数都需要进行格式验证
- 使用正则表达式验证特殊格式字段
- 对敏感数据进行加密存储

### 5.3 访问控制
- 基于角色的访问控制(RBAC)
- 不同模块操作需要相应权限
- 记录所有操作日志

### 5.4 网络安全
- 使用HTTPS协议传输
- 支持API限流和防DDoS攻击
- 定期更新安全补丁

## 6. 性能规范

### 6.1 响应时间
- 查询接口响应时间 < 200ms
- 创建接口响应时间 < 500ms
- 控制接口响应时间 < 1000ms

### 6.2 并发处理
- 支持1000并发连接
- 使用连接池管理数据库连接
- 异步处理非关键操作

### 6.3 缓存策略
- 使用Redis缓存热点数据
- 任务状态缓存5秒
- 配置信息缓存1小时

## 7. 相关文档

- [用户管理API参考文档](./user-api-reference.md) - 用户管理相关接口
- [虚拟机API参考文档](./vm-api-reference.md) - 虚拟机管理相关接口
- [联邦学习任务管理API参考文档](./federated-task-api-reference.md) - 联邦学习任务管理相关接口
- [训练数据管理API参考文档](./training-data-api-reference.md) - 训练数据管理相关接口
- [模型版本管理API参考文档](./model-version-api-reference.md) - 模型版本管理相关接口
- [系统日志管理API参考文档](./system-log-api-reference.md) - 系统日志管理相关接口
- [WebSocket协议文档](../WebSocket/) - 实时通信协议
- [数据库表结构文档](../../database/database_schema.md) - 数据库设计
- [项目概述文档](../../project_overview.md) - 项目整体架构
# 联邦学习初始模型/数据集解耦后端重构计划（v1.5）

## 1. 背景与目标
- 文档更新要求初始模型在任务创建前完成生成/上传，`initialModelConfig` 成为任务创建与配置接口的唯一入口。
- 后端需要移除初始模型接口对 `taskId` 的依赖，引入模型绑定状态管理，并支持 `AUTO` / `CUSTOM` 双模式。
- 所有改动必须严格遵循团队“打卡机制”：每个任务节点在开始、暂停、完成时记录时间、执行人和摘要，确保可追溯。

## 2. 打卡机制要求
- 每个任务项在每日 standup 前后至少各一次打卡，记录内容包含：日期、进度百分比、阻塞点。
- 任何跨文件的接口签名修改必须同步更新打卡备注，说明涉及文件范围与影响模块。
- 未打卡的任务视为未启动，不得进入下一阶段。

## 3. 进度看板

| 阶段 | 任务项 | 主要文件/包 | 状态 | 负责人 | 最近打卡时间 | 备注 |
|------|--------|-------------|------|--------|--------------|------|
| VO/DTO 调整 | 更新 `InitialModelGenerateRequest` / `InitialModelUploadRequest`：移除 `taskId`、新增 `labels`、校验 `modelType` | `com.feduwacomm.model.dto.initial` | 未开始 | 待指派 | - | 同步请求体校验注解 |
| VO/DTO 调整 | 新增 `InitialModelBindingVO`，补充 `bindingStatus`、`bindings` 列表；扩展 `InitialModelDetailVO` | `com.feduwacomm.model.vo.initial` | 未开始 | 待指派 | - | 供 `GET /model/initial/{modelId}` 使用 |
| VO/DTO 调整 | 扩展 `FederatedTaskCreateRequest`、`FederatedTaskConfigDTO`：替换 `modelConfig` 为 `initialModelConfig` | `com.feduwacomm.model.dto.federated` | 未开始 | 待指派 | - | 需要新增 `InitialModelConfigDTO` 结构定义 |
| Controller 更新 | 调整初始模型控制器路径：新增 `GET /initial/{modelId}`、`GET /initial/task/{taskId}`、`POST /initial/task/{taskId}/distribute`、`GET /initial/{modelId}/download`、`DELETE /initial/{modelId}` | `com.feduwacomm.controller.InitialModelController` | 未开始 | 待指派 | - | 需更新 Swagger 注解与权限校验 |
| Controller 更新 | 更新联邦任务控制器：解析 `initialModelConfig`、校验 `datasetId` 必填、在响应组装 `configSummary.initialModel` | `com.feduwacomm.controller.FederatedTaskController` | 未开始 | 待指派 | - | 注意 request/response 映射器同步调整 |
| 业务逻辑 | 解耦初始模型生成逻辑：`InitialModelService` 支持无 taskId 场景，维护绑定记录表/字段 | `com.feduwacomm.service.impl.InitialModelServiceImpl` | 未开始 | 待指派 | - | 需要新增绑定关系持久化方法 |
| 业务逻辑 | 更新任务创建流程：在 `mode=AUTO` 时自动生成模型并记录 `autoGenerated=true`；`mode=CUSTOM` 校验模型状态 | `com.feduwacomm.service.impl.FederatedTaskServiceImpl` | 未开始 | 待指派 | - | 确保事务范围覆盖模型绑定 |
| 业务逻辑 | 调整模型分发、删除、下载逻辑以 `modelId` 为主键；任务删除时解除绑定 | `com.feduwacomm.service.impl.InitialModelDistributionService` 等 | 未开始 | 待指派 | - | 需同步更新仓储接口 |
| 验证 & 日志 | 更新审计日志与事件通知，记录模型绑定/解绑操作及自动生成动作 | `com.feduwacomm.audit`, `com.feduwacomm.log` | 未开始 | 待指派 | - | 打卡备注需附带操作事件ID |
| 验证 & 日志 | 回归联邦任务与初始模型相关集成测试，补充 `AUTO` / `CUSTOM` 场景 | `feduwacomm-server/src/test/java` | 未开始 | 待指派 | - | 测试执行也需打卡 |

> 说明：如需拆分更细粒度子任务，可在原任务项下追加“子打卡”记录，但主任务的状态必须与打卡记录一致。

## 4. 风险与依赖
- **Schema/数据库变更**：若现有表未保存模型绑定信息，需新增字段或表（例如 `initial_model_task_binding`），应提前评估迁移脚本。
- **前端兼容性**：接口输出结构变更需同步前端，建议在控制器任务完成时更新打卡备注并通知前端负责人。
- **自动生成策略**：需确认业务是否已有默认架构模板，避免 `AUTO` 模式缺省导致运行时异常。

## 5. 打卡记录模板
```
[2025-10-05 09:30] 阶段：VO/DTO 调整 - InitialModel DTO
进度：30%
摘要：完成请求体字段重构，等待统一校验器更新
阻塞：待确认 labels 最大长度限制
```

完成上述模板填写后，需同步到团队打卡渠道，确保可追溯。
